export enum ApproverType {
  USER = 'user',
  ROLE = 'role',
  DYNAMIC_ROLE = 'dynamic_role'
}

export enum DynamicRole {
  HOD = 'hod',
  CFO = 'cfo',
  CREATOR = 'creator',
  ASSIGNEE = 'assignee'
}